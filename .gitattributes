# Normalize line endings: LF in the repo for all text, so CI (Linux) and local
# Windows checkouts agree. Shell scripts must stay LF; PowerShell is fine as-is.
* text=auto eol=lf
*.ps1 text eol=crlf
*.png binary
*.exe binary
*.dll binary
